import { questionFeatured } from 'emberboard/helpers/question-featured';
import { module, test } from 'qunit';

module('Unit | Helper | question featured');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionFeatured([42]);
  assert.ok(result);
});
